# 五连图生成逻辑复盘与优化方案 v0.1

日期：2026-09-06。状态：分析与待实施方案；本次仅修改生成提示的辅助文字样式，未重构生成引擎、未触发付费生图、未发布线上。

## 1. 结论与证据

一句话理解：现在完成了“生图、排字、切图”，但没有完成“整体设计母版”；需要更换设计组织方式，而非只加强一句风格一致的提示词。

| ID | 证据 | 可以确认的结论 |
|---|---|---|
| E01 | 用户图1、图2 | 提示条过于抢眼；成品含门头、独立场景及重复黑色文案框 |
| E02 | 用户图3、图4 | 稿定编辑器显示6000×900画板，背景、文字/组合、菜品元素可选择；不能据此确认其内部技术架构 |
| E03 | 用户图5—11 | 蓝色海鲜、绿色田园、暗色日料、红色纯文字、墨绿中餐、红色湘菜、深蓝粤菜等完整横幅设计 |
| E04 | backend/app/services/design_plan.py | 第一格明确写“门头或品牌标识”，第五格安排环境；固定五段叙事而不是完整横幅构图 |
| E05 | backend/app/services/image_generation.py | 参考图取排序后的前三张，未按用途隔离；提示词允许门头/环境；每格固定黑框排字，长标题按字符截断 |
| E06 | 本地实际任务 a3e928f1-5cb5-4363-bd57-95bf55189892 的PNG核验 | long.png为4000×600；01—05.png均为800×600，逐像素等于长图对应裁切区域 |
| E07 | backend/app/api.py 与生成服务 | 任务通过进程内BackgroundTasks执行；暂停状态在模型返回等阶段检查，不等于撤销模型请求 |

冲突说明：用户看到的“不像一张长图裁切”是正确的视觉质量判断；E06确认技术上已经裁切。根因在于裁切前的母图被做成了五块照片式场景。不要把问题归因于用户上传案例不够。

访问 https://m.gaoding.com/ 未成功获取页面，本报告对稿定的判断以用户截图为依据。会员权益范围、导出收费细则、内部模型/数据结构未验证。

## 2. 当前链路的问题

一句话理解：素材用途、构图任务和排版引擎三处都在引导错误结果。

1. 门头既被当作事实识别素材，也可能作为视觉参考传给生图模型；设计计划又主动要求门头入画。这不是单独补一句负面提示词能可靠解决的。
2. “品牌开场—主推菜—卖点—套餐—到店”规定了五个不同场景，天然接近照片故事板。首页五连图不应强制每套都遵循这个顺序。
3. 所有风格最终落到相同坐标的半透明黑框、相同字体、相同截断规则，没有模板专属构图能力。
4. 一张模型生成的位图无法保留独立菜品、背景、字组等编辑层；“本地排字”方向可保留，但应升级为母版级图层渲染。
5. 截图输入出现饺子，需求却写酸菜鱼；输出又有饮料和室内环境。应核实素材与需求是否匹配，并核查套餐明细，不应把模型补全当作门店事实。
6. 对任意源图强制ImageOps.fit为20:3存在裁掉主体风险，应先校验构图和尺寸，再决定扩展背景或返回重做。

## 3. 稿定值得借鉴的机制

一句话理解：借鉴“设计好的完整母版＋可替换图层”，不是复制其受版权保护的模板或建设全功能编辑器。

| 观察到的行为 | 可能目的（推断） | 我们应复现的机制 | 团绘的差异化 |
|---|---|---|---|
| 一个连续横幅与统一背景（E02/E03） | 减少风格漂移 | 一个母画布统一配色、光影、装饰和字体 | 自动把门店信息填入完整设计 |
| 菜品与文字可以选择（E02） | 降低局部修改成本 | 独立图层、素材绑定、局部重排 | 改价格不用重新付费生图 |
| 多种横幅模板（E03） | 缩短从空白到成品的时间 | 分类模板、适配规则与替换槽位 | 按菜系、素材数量、品牌色匹配 |
| 标题位置与菜品尺度有主次（E03） | 提高辨识度与食欲感 | 一张主视觉、层级明确的文案和辅助菜品 | 自动检查文字与切缝、主体是否冲突 |

上述目的尚无转化数据支持。不要复制水印、品牌文案、受限字体与模板文件；用户截图可用于内部分析，不能默认具备商业素材授权。会员机制不是首轮核心依赖。

## 4. 新生成链路（建议设计）

一句话理解：AI理解需求、处理视觉素材；确定性排版工具负责完整设计和准确交付。

确认门店事实 → 素材角色校验 → 选择母版与锁定风格 → 生成/处理必要素材 → 在一个母画布排版 → 质量检查 → 导出长图并裁切五图。

### 素材规则

- 门头：仅供识别店名、行业与品牌线索，不得传入成品视觉生成参考或渲染图层。
- 菜单：仅提取已确认菜名、价格、规格，不能把整张菜单拍照塞进五图。
- 菜品：绑定明确的菜名；优先真实照片抠图、适度增强，再合成；缺失素材时要求补图，或经用户确认使用示意图。
- Logo：单独上传或经确认提取的品牌资产；允许作为Logo图层，不等于允许门头照片入画。
- 环境：只有用户提供并明确选择展示时使用，不能凭空生成后当作真实店内环境。
- 参考案例：只提供风格/构图分析，不混入用户菜品池，不直接复用未授权内容。

### 母版契约

以下是拟新增的数据契约，不代表现有字段已经实现：

| 对象 | 必须约束的内容 |
|---|---|
| DesignSpec | 项目、事实版本、模板ID/版本、风格ID、画布尺寸、导出规格 |
| StyleTokens | 主辅色、背景、字体、标题层级、装饰语言、阴影/光照规则 |
| Layer | 类型、位置、尺寸、层级、素材ID或事实字段绑定、可编辑属性 |
| AssetPolicy | 允许出现在成品中的素材白名单；门头/菜单原图禁止渲染 |
| SliceRules | 五个裁切窗口、切缝安全区、文字及关键主体保护范围 |
| Validation | 事实一致、素材匹配、无溢出、无切字、五图与长图像素一致 |

母画布沿用20:3；例如4000×600切为五张800×600。截图中的6000×900也属于同一比例，但正式交付分辨率应按投放平台要求确认。切片只是交付窗口，不是五个独立设计任务。背景和装饰允许跨切缝；重要文字与主体应避开切缝，同时检查单张观看是否完整。

建议首套母版采用“品牌主题文字＋主推菜＋辅助菜品”的连续构图，菜品数量由真实素材决定；不能为了填满五个窗口虚构四道菜。纯文字类应为独立模板类型。

### PE职责与工具边界

- 理解层：输出已确认事实、冲突、缺失信息与素材用途；冲突未解决不能进入付费生成。
- 设计规划层：输出受schema约束的DesignSpec，不再输出五个独立摄影场景。
- 视觉生成层：只生成获准的背景/装饰或经确认的视觉素材，接收明确白名单，不接收全部上传图片。
- 排版层：依据模板摆放真实菜品、Logo和文字；按字体度量换行/缩放，不能静默截断店名、价格等事实。
- 验收层：渲染前检查事实及引用；渲染后检查切字、主体遮挡、整体一致性；失败时阻止“成功交付”。

推荐最小架构为一个需求理解Agent＋结构化设计计划＋素材处理/排版/裁切工具＋确定性状态机，无需先引入多Agent。这里只调整工程职责，不宣称稿定采用同样实现，也不需要先更换当前模型。

## 5. 退出、暂停与恢复

一句话理解：应支持离开页面后继续，但不能把离开、暂停、取消和重新付费混为一谈。

现状：模型任务在后端进程执行，关闭网页不会自动取消；这不等于已有可靠的云端任务队列。进程重启、电脑休眠或本地服务退出后的恢复没有保证。

建议：

- 点击确认后创建唯一任务；离开页面不新建、不取消、不重复扣费。
- 回到项目读取原任务和结果；保存输入快照、模板版本、状态、产物与调用记录。
- 使用持久化任务队列、心跳/失败恢复、幂等请求，防止刷新和重复点击重复提交。
- “暂停”指停止后续阶段；已发往模型的请求可能继续执行及计费，UI必须明确。
- 后续实现检查点：保留已完成视觉素材，恢复时从排版继续，避免重复生图；当前实现不能承诺无损续跑。
- 在提供商明确支持取消前，不展示“取消即不收费”承诺。

一键生图在首页确认后走上述引擎；专业创作保留首次成型版的逐步确认，两者共享同一DesignSpec和渲染器。装修全案保持独立的后续项目级入口，不在本轮混入单次五图流程。

## 6. 实施顺序与验收

一句话理解：先用一个原创母版证明正确，再扩充模板和编辑能力。

| 优先级 | 用户结果 | 实施内容 | 验收 |
|---|---|---|---|
| P0 | 不再出现错误门头/虚构套餐 | 素材用途隔离、白名单、事实冲突门禁；缩小辅助提示 | 上传门头仍可识别，但所有渲染引用不含门头；输入不符明确提示 |
| P1 | 得到真正统一的五连图 | 一套原创分层母版、结构化设计计划、连续背景、文字度量排版 | 长图整体风格通过人工验收；五张逐像素等于长图裁切；无关键文字切断 |
| P1 | 离开后可以找回结果 | 持久任务、项目恢复、幂等、暂停检查点 | 关闭页面重进是同一任务；反复点击不新计费；进程异常有明确恢复状态 |
| P2 | 方便改字换图与选更多风格 | 图层局部编辑、多模板、项目风格记忆 | 只改价格不调用生图；项目风格可锁定，用户可明确覆盖 |

建议先用3个真实项目验证：菜品齐全、只有一道菜、素材冲突。保留完整长图/五切片、DesignSpec、素材引用、失败原因、耗时和模型调用次数。优先统计首轮设计通过率、事实错误率、人工修改次数与重复调用，而非只统计“模型成功返回”。

素材库准备建议：首批精选10—20套有代表性的五连图参考即可，不是上线硬性数量。每套标注行业/菜系、配色、标题位置、菜品数量、适用场景、授权来源；实际门店照片/Logo归各自项目，公共模板与私有资产分开。案例库指导模板设计，不等于把截图全塞给模型，也不默认用于训练。

待确认：首个原创母版的视觉方向、真实测试菜品及套餐明细、目标投放尺寸、是否允许缺图时使用示意图。无需等素材库很大才修复当前逻辑。

# 团绘AI技术适配声明

> 版本：V0.1评审稿  
> 日期：2026-08-28  
> 状态：待审核，未授权开发  
> 产品依据：`团绘AI_PRD_v0.6_需求修正版.md`  
> 技术依据：`AI产品Vibe Coding通用技术栈手册.md` V2.1  

## 1. 产品形态判断

### 1.1 产品类型与核心任务

团绘AI是一个分阶段确认的多媒体创作与平台发布产品。用户上传菜单、门头及补充图片，系统识别门店事实；信息不足时进行第二轮定向追问；用户确认事实、五图方案和生图Prompt后，系统生成一张20:3连续母版，确定性写入中文文字，裁切为5张4:3图片，同时生成Logo与品牌介绍图，校验后导出或发布至抖音、美团。

BD是首批种子用户群体，不是独立产品角色，不建设BD专属入口或技术通道。

### 1.2 核心交互

- 多轮对话：缺失店名、定位、主推内容时定向补问；
- 分阶段确认：经营事实、品牌/价格/Logo、五图方案、Prompt、最终发布均需人在环；
- 长任务异步：OCR、视觉分析、生图、排版、校验和发布状态同步；
- 可视化操作：连续母版与五张切图预览、局部重做、文字修改；
- 外部高风险动作：平台账号授权和直接发布。

### 1.3 开发路径

采用“纵向切片”，不采用纯后端先行。

原因：图片质量、跨切线连续效果、事实确认和二轮对话必须通过真实界面才能判断；若只输出API JSON，无法验证产品核心价值。每个阶段仍要求后端能力可通过API独立测试，前端只做当阶段必需的真实交互。

### 1.4 建议阶段

| 阶段 | 目标 | 阻断关系 |
| --- | --- | --- |
| Stage 0 | 平台接口/资质、模型、20:3渲染和中文文字技术Spike | 未通过则调整范围，不进入全面开发 |
| Stage 1 | 上传→分析→缺口追问→事实确认纵向闭环 | 核心事实链路验收 |
| Stage 2 | 方案/Prompt确认→20:3生成→文字渲染→裁切→校验 | 核心视觉链路验收 |
| Stage 3 | 编辑、版本、导出、平台授权与直接发布 | MVP业务闭环验收 |
| Stage 4 | 趋势采集后台、生产硬化、小程序迁移 | 前三阶段稳定后进入 |

当前仅输出技术文档，未开始任何阶段开发。

## 2. 采用的默认方案

| 能力 | 推荐方案 | 采用原因 |
| --- | --- | --- |
| 后端 | Python 3.11.x + FastAPI | 符合手册默认基线，适合AI、OCR、图像处理和可测试API |
| 数据校验 | Pydantic v2 | 约束API、模型结构化输出和工具参数 |
| 后端测试 | pytest | 支持mock、状态机、权限和错误路径测试 |
| 前端 | Next.js + TypeScript + App Router + Tailwind CSS | Web/WAP统一终端，便于响应式交付与后续API复用 |
| API | `/api/v1` REST；长任务采用“提交任务+查询状态” | 任务耗时长且需要断点恢复，不依赖单次HTTP连接 |
| 进度更新 | Stage 1先轮询；需要更实时体验时增加SSE | 避免第一阶段同时维护两套异步机制 |
| 编排 | 确定性代码状态机 + 有限AI决策节点 | 主流程固定；AI仅负责识别、归纳、策划和Skill路由 |
| Prompt | 独立模板、版本化、输入变量白名单 | 支持审计、回滚、安全隔离和效果对比 |
| 版本管理 | Git + Python/Node锁文件 | 保证可恢复和环境可复现 |

## 3. 触发的按需模块

| 模块 | 触发需求 | 结论 |
| --- | --- | --- |
| PostgreSQL | 多用户、多项目、事实/资产版本、授权、发布事务和并发修改 | 从Stage 1采用，不使用SQLite作为业务真源 |
| SQLAlchemy + Alembic | 关系模型与数据库迁移 | 从Stage 1采用 |
| 对象存储适配层 | 原图、母版、切图、Logo和导出包体积大 | Stage 1本地存储实现统一接口；测试/生产切对象存储 |
| OCR/多模态模型 | 菜单、门头和图片理解 | Stage 0选型，Stage 1接入 |
| 图像生成模型 | 20:3视觉母版、Logo与品牌图 | Stage 0评测，Stage 2接入 |
| Pillow/图像处理 | 裁切、拼接、像素一致性、格式转换 | Stage 0验证，Stage 2正式采用 |
| 确定性排版引擎 | 中文文字准确和字体回退 | Stage 0验证；优先服务端渲染，具体库评测后确定 |
| 持久化任务队列 | 生图、校验、导出、发布为长任务 | Stage 1先建立持久化任务表；Stage 2根据并发测试决定队列实现 |
| 平台发布适配器 | MVP直接发布抖音、美团 | Stage 0验证官方接口与资质后逐平台接入 |
| 调度器 | 后续每日趋势采集 | Stage 4引入；首期只做关闭状态的配置入口 |

## 4. 偏离或暂缓的默认方案

### 4.1 偏离：不采用纯后端先行

替代为纵向切片。影响是Stage 1即建立最小正式Web/WAP页面，但只覆盖上传、分析、追问和事实确认，不提前开发完整视觉工作台。

### 4.2 偏离：不以SQLite作为MVP业务数据库

团绘AI存在账户、租户、项目、版本、任务、授权和发布事务，且种子阶段也会多人使用。直接采用PostgreSQL可减少后续高风险迁移。代价是本地环境多一个依赖服务。

### 4.3 暂缓：Redis/Celery等专用队列

Stage 1以数据库任务表、Worker和抢占锁验证恢复语义；当出现多Worker、明显队列积压或吞吐瓶颈时，引入Redis队列。不得仅为架构完整预装。

### 4.4 暂缓：RAG/向量数据库

平台规则、字体许可、行业模板和Prompt版本均属于结构化、可版本化知识，首期用PostgreSQL与文件版本管理即可。只有案例特征规模与语义检索质量证明需要时才引入pgvector；不引入Chroma。

### 4.5 暂缓：小程序

首终端为统一Web/WAP。小程序只复用后端API、数据契约和设计令牌，待Web/WAP核心闭环验收后另写迁移技术文档。

## 5. 强制底线检查

| 项目 | 状态 | 设计要求 |
| --- | --- | --- |
| 密钥与隐私 | 方案通过，实施待验 | Key仅后端读取；`.env`不入库；生产使用Secret；日志脱敏 |
| 数据与任务恢复 | 方案通过，实施待验 | 事实、确认点、任务journal和发布状态全部持久化 |
| 输入输出校验 | 方案通过，实施待验 | 文件真实类型检查；Pydantic校验AI/API输出；OCR事实需确认 |
| 错误边界 | 方案通过，实施待验 | 统一错误结构；不返回堆栈、密钥或第三方原始敏感响应 |
| 高风险动作 | 方案通过，实施待验 | 发布、删除、跨项目复用由确定性权限代码控制并二次确认 |
| Prompt安全 | 方案通过，实施待验 | OCR/网页/用户文本作为不可信数据；不能覆盖系统政策和工具权限 |
| 测试真实性 | 计划明确，尚未执行 | 每种模型契约同时执行mock测试和真实Key端到端冒烟 |

## 6. 需要产品经理决定的问题

以下问题会显著改变成本、安全或交付范围，开发前必须确认：

1. 首个确保打通的发布平台：抖音、美团或必须同步完成两端；
2. 最终发布确认权：项目操作者凭授权确认，还是必须由平台账号持有人确认；
3. Stage 1首发行业：建议只做餐饮，以便建立可控OCR和评测集；
4. 模型数据边界：是否允许把菜单、门头上传至第三方模型，以及允许的模型服务地区；
5. 单项目模型成本上限、允许的整套重做次数；
6. 原始素材、生成资产和审计记录的保存期限；
7. Logo在MVP是否必须同步完成，还是允许作为不阻断五图的异步资产。

## 7. 开工前外部阻断项

- 获取抖音、美团最新官方图片规则和发布接口文档；
- 确认应用申请条件、Scope、测试账号/沙箱、回调和审核周期；
- 准备至少10组授权、脱敏的餐饮菜单/门头/菜品样本；
- 确认候选OCR、多模态、图片生成和视觉审核服务可在中国大陆稳定调用；
- 确认字体商用授权清单。

在上述事项审核前，本声明不授权创建代码、安装依赖、调用付费模型或申请平台权限。

# 团绘AI技术适配声明 V0.2｜Stage 2A

> 日期：2026-08-31  
> 产品依据：`团绘AI_PRD_v0.7_竞品启发优化版.md`  
> 技术依据：`AI产品Vibe Coding通用技术栈手册.md`  
> 当前结论：允许在现有Stage 1基础上做纵向切片增量开发，不整体重构。

## 1. 产品形态判断

- 产品类型与核心任务：餐饮门店视觉包生成产品；本阶段先保证真实素材能形成可追溯、可确认的门店事实卡。
- 核心交互：多图上传、素材角色/优先级、异步分析、定向补问、人工确认。
- 开发路径：纵向切片。素材语义和事实确认必须通过真实界面验证，同时保留独立API测试能力。
- 当前阶段范围：Stage 2A“素材语义化与真实分析接入”；不提前实现生图、20:3排版、Logo、导出和发布。

## 2. 采用的默认方案

- 延续FastAPI、Pydantic、SQLAlchemy、Alembic、pytest：现有契约合理且已有测试。
- 延续Next.js、React、TypeScript：在现有页面扩展正式纵向切片，不创建平行临时页面。
- 长任务继续使用“提交任务+轮询”：与现有API兼容，任务状态已经持久化。
- 本地继续SQLite，生产目标仍为PostgreSQL：本阶段不进行高风险数据库切换。
- 使用`httpx`调用百炼OpenAI兼容API：依赖已存在，不新增SDK。

## 3. 触发的按需模块

- OCR/视觉模型：由菜单、门头真实识别触发；本阶段实现百炼适配器和mock适配器。
- 模型调用记录：由成本、耗时、错误审计触发；新增最小`model_call_records`表。
- 素材语义元数据：由V0.7素材角色、优先级、主视觉触发；扩展`source_assets`。
- Prompt文件：由手册的版本化要求触发；新增独立Prompt模板。

## 4. 偏离或暂缓

- 暂缓专用任务队列：当前单机验证量不足；保留持久化任务和重启恢复，达到多Worker需求后再引入。
- 暂缓对象存储：本地文件接口已满足本阶段；生产部署前迁移。
- 暂缓正式生图双供应商：本轮只准备配置位，不产生图片费用；千问默认、豆包失败切换在视觉生成阶段实现。
- 暂缓Tailwind迁移：现有CSS可满足当前页面，迁移会制造无产品收益的改动。

## 5. 强制底线检查

- 密钥与隐私：Key仅从后端`.env`读取；`.env`已被忽略；API和日志不得回显。
- 数据与任务可恢复：素材元数据、事实版本、人工确认、任务、模型调用记录进入数据库。
- 输入输出校验：上传内容检查继续保留；模型输出经宽容JSON解析和Pydantic校验。
- 错误与日志边界：第三方错误映射为受控错误码；不向前端返回堆栈或原始敏感响应。
- 测试与真实模型验收：先跑mock自动化；因项目根目录当前无`.env`，真实百炼冒烟在用户填Key后执行，未执行前不得宣称真实模型通过。

## 6. 需要产品经理决定的问题

无。本阶段不新增供应商、收费能力或终端；使用的百炼供应商和餐饮范围均已确认。真实模型冒烟仍需要用户在本机填写Key和Base URL。


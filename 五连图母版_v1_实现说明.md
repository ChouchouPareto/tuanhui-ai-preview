# 五连图母版 v1：实现边界与测试

本轮为本地代码优化，未发布GitHub，未进行付费模型调用。

## 已实现

- 生图模型只生成低对比连续背景，不再接收上传照片和旧版分屏场景指令。
- 素材白名单同时检查product类型与dish/signature_dish角色；门头、菜单、环境不参与成品合成。
- 在4000×600母画布上统一背景、配色、文字与真实菜品；不再给每格套黑色文字框。
- 四种现有风格对应确定性配色；单个原创母版，暂不意味着已有多种不同构图模板。
- 真实菜品完整缩放放入素材框，最多使用按主推/优先级排序的前三张；不是自动抠图，不会凭空补菜。
- 一张菜品时为中央单主体，不虚构其他菜品；如果要求每个切片都有菜品，需补充素材或后续设计专门的单菜连续构图。
- 文字按实际宽度换行、缩小；放不下会报错，不再截断店名与价格。
- 模型调用前验证图片可读性与文案容量；无合格菜品时不调用模型。
- 每次输出design-spec.json记录模板版本、文案、风格、引用素材ID与切片坐标；这是审计清单，不是完整可编辑画布文件。
- 保留两个创作入口的原有流程；没有修改既有项目和生成结果。

## 尚未完成，不能据此承诺

- 真实照片自动抠图、菜品语义与文字需求自动比对、生成背景内容审核。
- 持久化任务队列、服务重启恢复、暂停后的无损续跑。
- 前端分层编辑器、Logo图层、多套构图模板。
- 真正调用模型后的美术验收；本轮验证使用无费用测试图片，不代表商业设计效果已达标。

## 测试方式

后端测试应在临时工作目录执行，设置PYTHONPATH指向backend，再运行pytest backend/tests的绝对路径；现有测试夹具会清理相对data目录，不要在含用户测试上传的项目根目录运行。

新增覆盖：素材白名单、旧分屏指令不进入背景提示、真实菜品像素保留、五张裁切逐像素一致、设计清单、超长文字报错、缺菜品零模型调用。

下一步用真实菜品发起一轮测试，先看整张长图的背景、菜品比例、文字层级，再看五张切片。不满意时调整此母版，不另起一套创作流程。

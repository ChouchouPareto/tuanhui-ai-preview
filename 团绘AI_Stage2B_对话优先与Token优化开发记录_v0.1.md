# 团绘AI Stage 2B 对话优先与 Token 优化开发记录 v0.1

日期：2026-08-31

## 1. 产品决策

图片识别保留为辅助能力，但不再作为默认主流程。默认由用户通过对话提供店名、定位、主推菜品或套餐、真实卖点和主推价格；只有用户明确选择AI辅助时才调用百炼。

## 2. 新主流程

```text
上传菜单与门头
→ 点击“开始整理门店信息”
→ 零模型调用生成第一轮最多3个问题
→ 用户回答
→ 继续追问剩余强事实
→ 用户确认事实卡
```

可选分支：

```text
用户点击“AI辅助识别全部素材·消耗额度”
→ 页面提示将产生模型费用
→ 用户再次确认
→ 调用百炼
→ 返回菜单/门头候选
→ 用户确认强事实
```

## 3. 本次实现

- 分析接口新增`use_ai`开关，默认`false`。
- 默认创建`dialogue_intake`任务，不调用任何模型。
- 每轮追问最多3项；新增主推价格强事实。
- 首页默认按钮改为“开始整理门店信息”。
- AI按钮明确标注“消耗额度”，调用前二次确认。
- 门头候选、多语言合并及用户选择能力继续保留。
- 菜单结构新增菜名、分类、原始价格、数字价格、单位、规格、套餐内容、证据、坐标、置信度和待确认标记。
- 缺少原始价格证据时，后端丢弃孤立数字价格。
- 价格与套餐内容强制标记为待人工确认。

## 4. Token控制效果

- 普通用户完成默认流程：模型调用0次。
- 只有用户主动选择AI辅助时产生模型调用。
- 已填写事实不依赖模型猜测。
- 复杂菜单后续采用用户指定/框选目标区域，而不是默认全菜单结构化。

## 5. 验证

| 项目 | 结果 |
|---|---|
| 后端自动化测试 | 15 passed |
| Python编译检查 | 通过 |
| TypeScript类型检查 | 通过 |
| Next.js生产构建 | 通过 |
| 默认对话任务模式 | `dialogue`，不调用分析器 |
| AI显式开关 | 已实现，并带费用确认 |

## 6. 暂定边界

当前先保证逻辑跑通。复杂菜单的全量识别不是默认路径；在进入五图策划前，只要求用户确认本次主推菜品或套餐及其真实价格、内容和卖点。后续再增加框选区域识别与调用缓存，不阻塞当前闭环。

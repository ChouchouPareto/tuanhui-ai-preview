# 持久队列与路由实测

主线 R6，关联 R02/R04/R11/R12/R20。当前批次是后端代码与离线验证，不是整个产品完成或生产发布。

## 原因与处理环节

1. 专业入口此前通过请求内 BackgroundTasks 执行，任务落库不等于服务退出后可被接管。现改为 WorkflowTask 与 ProfessionalGenerationJob 同事务保存，API 不执行生图；worker 条件领取，记录租约/token。
2. 只保存计划未覆盖实际素材变化。入队记录素材ID、名称、SHA256、角色/用途、优先级、主推和类型；执行前重新核对字节及元数据，不一致在模型调用前返回 EXECUTION_INPUT_CHANGED。计划、事实、项目与供应商仍复核 R5 合同。
3. 已成功任务与队列记录不是同一个提交边界。中断恢复先保留数据库中已确认的终态，仅将仍待执行/运行任务改为待核对；两入口都不自动重发远端未知请求。
4. 模型响应可能晚于停止或租约失效。共享执行器复查任务状态，已停止/待核对结果不再发布成功；本地排字分支同样复核。停止不承诺供应商未计费。
5. 路由末尾把无历史作品的描述当作新创作，管理/识别/旧资料操作也会落入该分支。新增保守拦截，动作未接通则澄清，不能声称已执行删除/识别。

## 本轮接口与运行变化

- 专业生成接口仍返回202与原 task_id；重复 request_id 返回原任务，不重复入队。
- 执行器离线时新请求503 WORKER_UNAVAILABLE，不遗留半个任务；旧请求仍可读取原任务。
- 新表由迁移 f4d8b0c2e6a1 增量建立。迁移拒绝破坏性降级；本轮只在测试库验证，没在用户业务库执行。
- worker 从两入口中选最早待执行任务；两类过期租约每轮都检查，避免一类长期排队阻塞另一类恢复。
- 用户停止专业任务同时关闭队列记录。QUEUED可领取，RUNNING过期转RECONCILING，终态不自动回QUEUED。
- 改动核心：`generation_contract.py`、`professional_queue.py`、`worker.py`、`image_generation.py`、`dialogue_routing.py`；新队列与新路由测试均在 backend/tests。

## 实际验证

命令 `.venv/bin/python -m pytest backend/tests -q`：243项通过（13.95秒）。R5基线210项，本批新增20项队列/恢复测试、10项首条动作保护、3项评测口径测试。

覆盖新请求与队列事务回滚、无worker、双领取、旧请求重放、素材变化、过期租约、已提交成功/失败保留、两入口调度、停止后/核对后晚到结果，以及新表增量重复执行。不是真实供应商重启或付费模型验收。

命令 `.venv/bin/python scripts/evaluate_intent.py`：在独立临时数据库通过真实 `/dialogue/route` 接口执行40条人工种子案例；禁外网、未使用业务库。结果见[当前报告](../evaluation/runs/20260917T162457.083106Z_0cbeef60.json)。

| 口径 | 结果 |
| --- | --- |
| 路由执行 | 40/40 |
| 意图标签归一化匹配 | 18/40；不是完整任务准确率 |
| 非生图意图进入生成准备 | 0 |
| 任务被修改、新增模型记录、目标错配或HTTP错误 | 0 |
| 完整业务动作验收 | 未运行，不计通过 |
| 真实模型质量/耗时/费用 | 未评测 |

首份报告 `20260917T162330.602729Z_db226bcb.json` 保留供对比；其 unexpected_image_preparations 的8条含E012误计：明确要求新图但暂未授权时，允许准备草稿，不等于执行收费调用。已修正评测器口径并加入测试。其余7条确实是错误准备分支，本轮已拦住；不能把“8→0”宣传成模型能力提升。18条意图匹配未提升，说明多数复杂动作仍需开发而非改标签掩盖问题。

## 上线与剩余限制

- 本轮未重启用户API/worker，未真实生图、未覆盖作品、未推送GitHub；正在打开的页面不保证已加载这些后端改动。
- 切换版本必须先核对在途任务、备份数据库、执行增量迁移，并一起更新API与worker；旧专业BackgroundTasks产生且没有队列项的任务不盲目补入队，须核对供应商记录。混跑新API/旧worker不是已验证兼容方案。
- 素材快照目前在执行前检查，不是内容寻址的不可变资产仓库；执行期间外部直接改文件尚需更强隔离。
- 旧未知调用的供应商查询/人工核对工具、完整授权有效期/金额上限仍待开发；持久队列不等于完成支付或生产身份隔离。
- 语义模型与父子工具网关、正式10～20模板、完整多画板及前端、Figma审核、真实图像评测均未完成。按原要求继续后端→Figma审核→前端→部署，不用这一批代替整体交付。

# v0.9.7 生成结果诊断与图片预览

日期：2026-09-12。范围：诊断已有结果、修复结果预览，不重跑付费任务。

## 结果诊断

任务 91e21226-4790-4506-8ff2-253884be17cb；设计方案 b7267b08-2303-43e3-9a86-962a4f6c5169。

- 保存的方案为 illustration，店名为山西面馆，hero_item、positioning、selling_points 为空，selected_asset_ids 为空。
- copy 中 headline、subheadline、price 为空，只有 store_name。
- image_generation.build_visual_prompt 对仅店名输入明确要求抽象氛围、禁止具体菜品；所有示意分支均禁止模型生成文字。
- 此次任务记录 provider=qwen，model=qwen-image-3.0。排版程序仅绘制非空文字，并把同一张长图裁成五份；没有为每张自动规划内容。
- 已检查实际 long.png 和 design-spec.json，结果与上述规则吻合。SUCCEEDED 只表示技术处理成功，并非设计质量通过。

结论：背景图被作为完整作品交付，问题来自生成策略和内容规划，不是展示缩放或用户未使用标准提示词。应区分商业事实不可编造与创意画面可以设计，增加完整内容规划和交付质量检查；不能用增加必填项代替修正策略。本次未修改生成策略或覆盖原图。

## 已实现

- 新旧生成结果统一使用 GenerationGallery。
- 长图和切片点击打开原生模态预览，不再绑定下载。
- 独立下载链接；预览支持上一张、下一张、左右方向键、放大原图、适应窗口、Escape 和关闭按钮。
- 原生 dialog 管理焦点及背景交互；移动端自适应，操作目标至少 44px，保留可见焦点。
- 不改变项目、任务、费用或结果文件。

## 验证

- TypeScript 检查、生产构建通过。
- 真实已保存结果只读测试：任务/创作/项目入口、刷新、返回项目再打开；预览不下载、切换、放大、关闭、375px 布局及独立下载。
- 截图检查桌面和手机预览。
- lint 存量问题：page.tsx:308 的 set-state-in-effect 错误及 4 条历史警告；本次新增组件无 lint 报错。

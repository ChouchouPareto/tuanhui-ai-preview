# V0.5 对话确认浮层

基线：3f8b10f / m1-v0.4-single-input。
分支：feature/m1-conversation-review-v0.5。

## 交互调整

- 确认组件位于首页对话容器内。桌面浮层宽 420px，向上展开，不参与容器高度计算。
- 补问态仅显示标题和缺项；删除重复的价格、素材数、说明与无效生成按钮。
- 确认态展示摘要、素材入口、费用和使用权确认；生成授权规则不变。
- 关闭、外部点击、Esc 可以收起，状态入口可重新打开。选择缺项后收起并聚焦原输入框。
- 缩小原生勾选框，限定尺寸，避免被通用表单样式拉伸。
- 主推要求支持菜品、套餐、卖点或特色。设计计划可用卖点/特色作标题，不伪装成菜名；真实图片要求未放开。

## 验证

- 后端 50 项测试通过，包含卖点/特色替代菜品和歧义校验。
- 前端 13 项状态测试、TypeScript 通过。
- 实际本地桌面页面：展开/收起时输入区高度均为 416.5px，浮层宽 420px；关闭和重新打开可用；点击门店名称后焦点为 creation-prompt，浮层关闭。
- 已查看桌面截图。手机尺寸、真实付费生图及完整确认态仍需专项验收；不能以本次补问态验收代替。
- 用户资料未提交到 Git；未推送或公开部署。
